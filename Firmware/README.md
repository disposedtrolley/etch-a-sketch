# Firmware

This subdirectory contains firmware for the **etch-a-sketch**.

## Building

1. Clone the main `etch-a-sketch` repo.
2. `cd` into the `Firmware` directory.
3. Generate the `CMake` project by running `mkdir build && cd build && cmake ../`.
4. Run `make` to compile the `.elf` binary.
